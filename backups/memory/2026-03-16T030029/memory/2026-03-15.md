# 2026-03-15

## LCM Summarize Fix — Thinking Exclusion + Fallback Model (18:45-19:23 UTC)
- **Root cause:** `thinking` field was filling LCM DB with garbage chain-of-thought → intentionally removed
- **Fix:** thinking stays excluded. Added `LCM_SUMMARY_FALLBACK_MODEL` env var for fallback model when primary returns thinking-only output
- **Retry chain:** primary retry → fallback model → truncation (last resort)
- **TimeoutStopSec fix:** increased from 30s to 120s in systemd unit
- **LCM VACUUM:** DB reduced from ~51MB to 2.6MB, 351 messages, 7 summaries
- **Gateway restart:** 19:23 UTC, changes loaded, John confirmed working at 19:31 UTC
- **Lesson:** Don't second-guess intentional decisions with upstream code. Be faster.

## CloakBrowser Tested & Working (02:31-02:47 UTC)
- Stealth Chromium, 33 C++ fingerprint patches, MIT license
- Installed via npm, tested on server — all 6 tests passed
- BrowserScan: "Normal" (regular Playwright = "DETECTED")
- Binary cached at `~/.cloakbrowser/chromium-145.0.7632.159.7/`
- Plan: Use directly (Option A) when scraping bot-protected sites (X, LinkedIn, Indeed)
- vxtwitter + CloakBrowser = full X/Twitter coverage, no API needed

## Skills Audit (02:59-03:12 UTC)
- All skills (except xint) are in John's own repo — no upstream to update
- xint: already at latest v2026.3.7.2
- ClawHub lockfile had 2 phantom installs (gateway-auto-fix, self-evolve-agent) — cleaned
- deep-research-pro SKILL.md version updated 1.0.0 → 1.0.2
- baseline-kit: current at v1.0.0

## Skill Monitor Built (02:04-02:12 UTC)
- Inspired by Cognee tweet from John (self-improving skills for agents)
- Built lightweight skill health tracking — SQLite-based, human-in-the-loop
- **Components:** `skills/skill-monitor/` (SKILL.md + 3 scripts: setup, log-run, audit)
- **DB:** `data/skill-monitor/skill-runs.db` (skill_runs + skill_feedback tables)
- **Audit features:** success rate tracking, degradation detection (7d vs 7d), common errors, untracked skills
- **Logging hooks:** appended to all 4 existing skills (deep-research-pro, baseline-kit, systematic-debugging, review-driven-dev)
- Philosophy: detect & flag, never auto-edit SKILL.md — propose fixes, get approval
- Cognee's approach: full knowledge graph + auto-amend. Our approach: SQLite + trends + human approval = leaner, safer

## Gateway Restart Incident (01:00-01:15 UTC)
- Gateway received SIGUSR1 → graceful restart cycle
- Drain hit timeout (90s) → forced restart, one embedded run timed out
- **Root cause of failures during drain:** gateway refused new tasks while draining
- **LCM 401 errors:** invalid OpenAI API key on the summarization path
- **Fix:** Switched LCM model to `openrouter/meta-llama/llama-3.3-70b-instruct:free` (avoids OpenAI key entirely)

## Mission Control Auth Fix (01:42-01:50 UTC)
- Mission Control couldn't connect to gateway WebSocket → "Link integrity" errors
- **Root cause:** missing `OPENCLAW_GATEWAY_TOKEN` env var → `operator.read` scope error
- Added token to MC's environment file, restarted service

## Wiki-links False Positive (01:50-01:55 UTC)
- Memory health tab showed "Link integrity: critical" with broken wiki-links syntax in content
- **Root cause:** session-memory hook saved a conversation summary that discussed wiki-links — health checker treated brackets in content as actual broken links
- **Fix:** Escaped bracket patterns in `memory/2026-03-15-0104.md` to plain text

## Watchdog Script Built (01:55-02:02 UTC)
- `scripts/watchdog.sh` — bash watchdog monitoring 4 components:
  1. Gateway health (HTTP `/health`)
  2. Mission Control (`next-server` process)
  3. LCM (db file write freshness, 30min threshold)
  4. QMD/Engram (plugin directory exists)
- Cron: `*/2 * * * *` with output to `/tmp/openclaw-watchdog.log`
- Per-component one-shot alerts + recovery notifications via Telegram
- Reads bot token from config via `jq`

## Config Changes Made
- **LCM model:** changed to `meta-llama/llama-3.3-70b-instruct:free` (avoiding OpenAI 401)
- **MC env:** added `OPENCLAW_GATEWAY_TOKEN`
- **Cron:** added watchdog entry

## Open
- MC dashboard was updated (scoping, daily log exclusion) by previous session — changes in `/root/.openclaw/mission-control/`
- LCM model override was set via LCM config (need to verify persistence across restarts)

## Mission Control Updated (03:30-03:41 UTC)
- Pulled cc8fc84 → 2449363 (27 commits, 123 files, +25K lines)
- Key upstream additions: i18n (10 languages), first-time setup wizard, Docker fixes, auth/security fixes, persistent doctor banner dismiss
- 6 local patches preserved:
  - ✅ QMD index support (memory/graph/route.ts) — no upstream changes
  - ✅ Memory health path fix (memory/health/route.ts) — no upstream changes
  - ✅ Daily log exclusion (memory-utils.ts) — no upstream changes
  - ✅ CSP nonce (layout.tsx) — subsumed by upstream, dropped ours
  - ⚠️ Re-applied: doctor banner error-only filter, doctor "Errors: N" sanitizer, CSP unsafe-inline
- Dependencies: added next-intl 4.8.3, @vitest/coverage-v8
- Build: 89s compile, clean, no errors
- Service restarted, Ready in 115ms, DB migrations OK

## MC CSS Fix (03:46-03:50 UTC)
- CSS not loading after update — pages rendered as unstyled HTML
- **Root cause:** standalone mode (`output: 'standalone'`) — `next build` generates CSS in `.next/static/` but the standalone server reads from `.next/standalone/.next/static/`
- Static files weren't being copied after build
- **Fix:** Added `postbuild` script to `package.json` — auto-copies `.next/static` → `.next/standalone/.next/static` and `public/` → `.next/standalone/public/`
- Also added CSP `unsafe-inline` patch (harmless, makes CSP more permissive)
- **Mistake logged:** Assumed CSP nonce issue (spent 10min debugging nonces) when actual problem was simpler — CSS files didn't exist where server reads them. Classic M020.

## MISTAKES.md System Built (03:50-03:55 UTC)
- John pointed out: keep repeating same mistakes, debugging the same things
- Built structured mistake database: `MISTAKES.md` — 20+ entries across 7 pattern categories
- Categories: DEPLOY, CONFIG, DEBUG, SPAWN, THINK, APPROVAL, KNOWLEDGE
- Each entry: root cause (not just description), prevention check, repeat counter
- Added Pre-Action Protocol to AGENTS.md — mandatory check before risky actions
- Startup sequence now reads MISTAKES.md first thing each session
- Goal: catch mistakes BEFORE making them, not log them after

## Learning.md Deleted (04:07 UTC)
- learning.md removed — redundant with MISTAKES.md + MEMORY.md
- Unique content merged: gateway restart details → M002, everything else already covered
- Removed from AGENTS.md startup sequence (5 files → 4)
- README.md updated: MISTAKES.md replaces learning.md

## Wiki-Links False Positive (04:01 UTC)
- MC memory health showed "Link integrity: CRITICAL" for wiki-links in daily log
- `[[wiki-links]]`, `[[brackets]]`, `[[...]]` patterns in log content treated as actual broken links
- Fixed by escaping bracket syntax in `memory/2026-03-15.md`
- Note: the log about fixing wiki-links... caused the same wiki-links bug. Meta. 🦊

## Memory System Design Discussion (04:03-04:08 UTC)
- John asked how SOUL/MISTAKES/learning/memory work together
- Explained the flow: SOUL (values) → MISTAKES (rules) → learning (history) → better rules
- John asked if it's bulletproof — honest answer: no, self-enforced, novel mistakes still get through
- John noticed mistakes only grow, never graduate
- Proposed graduation tiers: 🔴 Active (always check) / 🟡 Watch (14+ days no repeat) / 🟢 Retired (30+ days)
- Not yet implemented — open loop

## Lesson
- "Keep it lean" applies to memory files too. If a file isn't changing behavior, it's bloat. learning.md was a good start that got replaced by MISTAKES.md.

## File Cleanup — SOUL.md / MISTAKES.md / AGENTS.md (20:24-20:43 UTC)
- **John's concern:** SOUL.md kept bloating, duplicated rules across files, not sure if files were actually used
- **SOUL.md slimmed:** 78 → 35 lines. Moved thinking discipline → MISTAKES.md M040, session state/wrap-up → AGENTS.md. Left just personality: Core Truths, Boundaries, Vibe, Continuity.
- **MISTAKES.md cleaned:** Removed duplicate Pre-Action Checklist (same as AGENTS.md Pre-Action Protocol), stale status notes ("Added to SOUL.md ✅", "added to AGENTS.md ✅"), corrupted text at bottom, M010 hard rule (duplicated AGENTS.md Red Lines). M060 prevention → "See Recall section in AGENTS.md".
- **AGENTS.md:** Added Session State section (moved from SOUL.md). No other changes.
- **Key insight:** Rules live in rules files. Personality stays in SOUL.md. No duplication.

## MEM0 Integration (21:00–22:00 UTC)

- **MEM0 skill built:** 4 scripts (add, search, list, delete) using hosted Mem0 Platform API
- **Key API discovery:** Search uses v2 endpoint (`/v2/memories/search/`) with `filters: {OR: [{user_id: "..."}]}` and `top_k` param — not v1
- **Add uses v1** with `"version": "v2"` in body, returns async PENDING status
- **All endpoints need trailing slashes** or API returns 301 redirects
- **Integration approach:** Skill-based (shell scripts), not full plugin. Agent is the integration layer — calls MEM0 + QMD (memory_search) + LCM in parallel
- **AGENTS.md updated:** Recall priority now includes MEM0, new MEM0 Integration section with when-to-search and when-to-store protocols
- **MEMORY.md updated:** MEM0 noted in Setup and System lines
- **No QMD/LCM changes** — zero risk to existing systems
- **6 memories seeded:** timezone, crypto timeframe, OpenClaw setup, Freddy identity, trading system, hunter-alpha model

## MEM0 Auto-Store Hook Fix (23:20-23:30 UTC)
- John asked to check if MEM0 saved last session memories — only 6 seed memories, nothing new
- Hook was registered and "ready" but failing silently: `No session file found — skipping`
- **Root cause:** hook defaulted to `~/.openclaw/workspace/sessions/` but sessions live at `~/.openclaw/agents/main/sessions/`
- **Fix:** updated `handler.ts` — default sessions dir + fallback to most recent `.reset.*` file
- Gateway restart needed to load fix + activate mem0 plugin tools
- John said "wrap up" — checkpointing state
