# 2026-03-16

## LCM Summary Model Fix (02:00-02:58 UTC)
- **Problem**: LCM summary model failing — nemotron returns thinking-only blocks, llama-3.3-70b-instruct:free rate-limited (8 RPM on OpenRouter)
- **Solution**: Added Kilo Gateway provider (`kilocode:default` auth profile), switched summaryModel to `kilocode/meta-llama/llama-3.3-70b-instruct:free`
- Kilo Gateway = OpenClaw built-in provider, proxies to OpenRouter with separate API key (336 models available)
- KILOCODE_API_KEY already in env, just needed auth profile + config change
- Gateway restarted at ~02:57 UTC

## Watchdog Updates
- Added model name to LCM summary notifications (from gateway logs, since LCM DB doesn't store model)
- Added rate-limit (429) detection to queue-watchdog alerts
- Added "all extraction attempts exhausted" error type
- Success tracker for retry succeeded events

## MEM0 Plugin
- Built and loaded, hook path fixed (was looking in wrong sessions directory)
- Disabled in allowlist (removed from plugins.allow) — lossless-claw only
- Hook still not producing output — needs investigation
