# 2026-03-16 (MDT)

**MEM0 auto-store hook:** Fully patched (handler.ts → scans active .jsonl by mtime if no sessionFile; reset fallback). Deployed via gateway restart (05:34 UTC). Logs: [mem0-auto-store] DEBUG on trigger.

**System:** Gateway PID 229771, stable. Model: openrouter/x-ai/grok-4.1-fast. MEM0: 6 memories (seeds), hook ready for test (/new → check journalctl + mem0-search.sh).

**Security:** 2 critical open (UI auth off, small models unsandboxed). Logs clean.

**Open:** Test hook → confirm sessionFile resolution + new memories added.
---

**Memory & Learning System Deployed (18:10 UTC):**
- 9 scripts: memory-profile.sh, memory-surface.sh, memory-extract.sh, memory-merge.sh, learning-check.sh, learning-graduate.sh, learning-log.sh, signal-decay.sh, learning-capture.sh
- SQLite DBs: `data/freddy.db` (signals + learnings tables), `LEARNINGS.md` generated from existing learnings
- Cron jobs: memory-extract (every 6h), signal-decay (daily 04:00), learning-graduate (weekly Sun 03:00)
- 14 signals added to DB, 14 learnings imported
- AGENTS.md updated with Pre-Action Protocol, Memory system, Integration Rule

**LCM Tuning (17:02-18:22 UTC):**
- summaryModel → openrouter/google/gemini-2.5-flash
- contextThreshold → 0.75, freshTailCount → 20, incrementalMaxDepth → 2
- LCM_SUMMARY_INSTRUCTIONS added to systemd service (prioritize decisions/technical details, deprioritize filler)
- Custom instructions not a valid openclaw.json key — must be env var only

**Security Hardening (18:46 UTC):**
- controlUi.dangerouslyDisableDeviceAuth → false
- Removed small model fallbacks (nemotron, llama-3.3-70b, qwen3-next)
- Set fallback to openrouter/google/gemini-3.1-pro
- Added lossless-claw + memory-core to plugins.allow

**Cleanup:**
- Deleted: MEM0 skill directory, openclaw-engram, SESSION_STATE.md, IDENTITY.md, MISTAKES.md symlink, health-watchdog.sh, lcm-summary-monitor.sh, docs/ directory
- Removed openrouter/auto from model aliases (was causing credit exhaustion)
- Watchdog.sh fixed to read summaryModel from openclaw.json directly
- 9 stale cron ghost sessions cleaned, /home/john/.openclaw duplicate dir removed

**Tailscale + Remote Access (19:03-20:13 UTC):**
- Tailscale installed, authenticated
- Gateway bound to tailnet (gateway.bind=tailnet, gateway.tailscale.mode=off)
- Mission Control (Next.js) served via Tailscale: https://srv1435474.tail57784d.ts.net:8443 → nginx:443 → 127.0.0.1:3000
- OpenClaw Control UI via Tailscale: https://srv1435474.tail57784d.ts.net (gateway direct)

**Key Learnings:**
- "Mission Control" = John's Next.js app at /root/mission-control/ on port 3000, NOT OpenClaw's Control UI
- openrouter/auto alias was a model alias that caused credit exhaustion — removed
- OpenClaw cron (sys-*) jobs unreliable — always use system crontab
- SIGUSR1 restart doesn't re-read systemd env vars — need full `openclaw gateway restart`
- LCM leaf summaries too verbose (~711 tokens) — need tighter instructions
- Mem0 plugin config corrupts openclaw.json — avoid, use shell scripts instead
