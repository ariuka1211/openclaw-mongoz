# 2026-03-21 — Techno4k 🛠️

## VPN/Proxy Cleanup (00:22 UTC)
- All VPN/proxy packages purged: Surfshark, Cloudflare WARP, OpenVPN, WireGuard, protonvpn config
- Xray process killed, binary + config removed
- All related systemd services disabled + removed
- Kept: Tailscale (legit networking) + nginx (web server)
- `apt purge --autoremove` confirmed clean

## Hermes Setup (17:04-17:44 UTC)
- `openfang` deleted (moved to trash)
- `hermes-agent` cloned from GitHub, setup complete (Python 3.11, venv, 155 packages, 93 skills)
- OpenClaw migration executed: 13 items migrated (soul, memory, user-profile, messaging-settings, model-config, 7 skills)
- Model set to `openrouter/xiaomi/mimo-v2-pro`
- Hermes gateway running on `@Hermes910_bot`, port functional
- **Warnings:** MEMORY.md and USER.md missing from `~/.hermes/memories/`, skills dir empty
- Migration report: `/root/.hermes/migration/openclaw/20260320T171634`

## Session Bloat Investigation (00:35-00:37 UTC)
- 106 session files in main agent, mostly ~2.8 KB each
- **Root cause: `session-watchdog` cron job** (ID `f47bf180`), was running every 10 min
- Cron job already disabled at Mar 20 ~13:32 UTC
- Old session files are leftovers — not heartbeat sessions
- Heartbeat is default-on (30m interval) but HEARTBEAT.md is empty (just comments), so likely skips runs per docs
- To fully disable heartbeat if needed: set `agents.defaults.heartbeat.every: "0m"` in openclaw.json
- Dead session-watchdog cron job could be deleted to clean up

## Unnecessary Processes Killed
- Surfshark, Cloudflare WARP, next-server all killed
- next-server respawned (being managed by something, possibly systemd)
- All confirmed dead after force kill

## System Health (Mar 20)
- Disk: 12% used (11G/96G) after VPN cleanup
- Server rebooted Mar 20 16:21 UTC after 9 days uptime (likely gateway hang/OOM)
- Gateway restarted 16:22 UTC, all 4 agents initialized
- Tailscale logged out (needs re-login)

## openclaw.json Edits (00:19 UTC)
- Removed `tools.deny` block from `main` agent — Maaraa now has exec/write/edit access
- Restarted gateway to apply

## AI Trader Model Change (20:03 UTC)
- Changed from `xiaomi/mimo-v2-omni` / `xiaomi/mimo-v2-pro` to `minimax/minimax-m2.5:free` (all 3 slots: primary, fallback, reflection)
- Updated config at `/root/.openclaw/workspace/lighter-bot/ai-trader/config.json`
- Restarted ai-trader systemd service

## Lighter Bot Pipeline (00:26 UTC)
- **Scanner** (`lighter-bot/scanner.ts`) → writes `signals.json`
- **AI Trader** (`lighter-bot/ai-trader/ai_trader.py`) → reads signals, uses LLM, writes `ai-decision.json`
- **Copilot** (`lighter-copilot/bot.py`) → reads decision, executes trades on Lighter exchange
- These are three stages of one pipeline, not competing bots
- Mzinho working on renaming

## LCM Summary Model Change (19:53 UTC)
- Changed from `openrouter/google/gemini-2.5-flash` to `openrouter/minimax/minimax-m2.5:free`
- Edit in `/root/.openclaw/openclaw.json` under `lcm.summarizer.summaryModel`

## Cron Job Rename (00:46 UTC)
- Renamed `rate-limit-monitor` → `tradingbot-ratelimit`
- ID: `9f2ebdcd-7ce2-42a2-acb4-c2345f6f07fc`
- Disabled, runs every 15 min, checks for 429 errors in Lighter bot

## Session Cleanup (00:44-00:50 UTC)
- Deleted dead `session-watchdog` cron job (ID: `f47bf180`)
- Trashed 96 small session files from main agent (watchdog leftovers)
- Cleaned 57 `.reset.` / `.deleted.` / `.bak` files from main agent sessions
- Main agent: 20 MB (106 files) → 728 KB (10 files)

## TODO on next session start
- Check all agent session sizes and report to John

## Session Cleanup — 01:29 UTC
- Trashed 25 dead session files (6 orphaned transcripts + 19 .deleted remnants)
- Freed ~1.5MB, sessions dir went from 37 → 12 files
- Orphaned: old cron sessions, aborted subagents, test spawns
- Session files trashed to /root/.local/share/Trash/files/

## LCM Cleanup — 01:33 UTC
- Removed 22 trashed session conversations from LCM (230 messages)
- Removed 41 slug-generator sessions (81 messages, internal system artifacts)
- LCM: 118MB → 115MB (remaining is legitimate conversation data)
- Backup created and trashed
- 226 conversations, 17,057 messages, 339 summaries remain

## SOP & Pre-Task Gate — 01:42 UTC
- Created SOP-BIG-TASK-GATE.md (5-question pre-flight, decision matrix, escalation levels)
- Added objective trigger checklist to all 4 agent SOUL.md files (binary yes/no, no judgment)
- Maaraa (main) got a new SOUL.md with coordinator-specific gate
- Trigger: ~20-30 tokens per session. Full SOP loads only when triggered (lazy)
- Updated AGENTS.md with step 2 "check if big" in How I Work

## SOP Approved — 01:46 UTC
- John approved the SOP + objective trigger system
- All agents ready: Maaraa, Techno4k, Mzinho, Blitz
- Per-session cost: ~100-140 tokens. On-trigger: ~1075 tokens.

## Session Wrap-up — 02:01 UTC
- Session cleanup: 25 files trashed, 63 dead sessions removed from LCM
- SOP + objective triggers deployed to all 4 agents
- Slash commands reference shared
- All changes live, no restart needed

## SOUL.md Fix — 02:13 UTC
- **Problem:** Edited the wrong SOUL.md — put truncated version in `agents/main/agent/SOUL.md` instead of `workspace/SOUL.md`
- **Fix:** Added Pre-Task Gate + smaller-piece delegation to `/root/.openclaw/workspace/SOUL.md`
- **Cleanup:** Truncated agent SOUL.md moved to trash
- Pre-Task Gate: 9 objective triggers before dispatching
- Delegation rule: split tasks across agents in parallel, don't serialize what doesn't need to be
- Other agents' SOUL.md files (blitz, coder, system) are fine — full versions in agent dirs

## Maaraa AGENTS.md + SOUL.md Cleanup — 02:35-03:07 UTC
- John shared a Grok-style pure orchestrator AGENTS.md template — reviewed it critically
- Issues: too rigid ("never do anything"), serializes parallel work, missing failure handling, assumes agents that don't exist
- Wrote revised version combining good parts with what works for our setup
- **Key discovery:** `agents/main/agent/` is NOT Maaraa's workspace — it's just a config dir
  - Maaraa's workspace = `/root/.openclaw/workspace` (inherited from `agents.defaults.workspace`)
  - `agents/main/agent/` only holds `auth-profiles.json` and `models.json`
  - SOUL.md and AGENTS.md in that dir were dead files — never loaded by OpenClaw
- Read OpenClaw docs: AGENTS.md = operational rules ("how to work"), SOUL.md = identity ("who you are")
- Separated merged SOUL.md back into two proper files:
  - `workspace/SOUL.md` — identity, beliefs, vibe only
  - `workspace/AGENTS.md` — session startup, delegation protocol, pre-task gate, memory rules, red lines, group chat behavior
- Trashed dead files from `agents/main/agent/` (SOUL.md, AGENTS.md)
- **Lesson:** workspace files ≠ agentDir files. Bootstrap files load from workspace, not agentDir.
